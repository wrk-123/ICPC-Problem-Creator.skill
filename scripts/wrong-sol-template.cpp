#include <bits/stdc++.h>

using namespace std;

int main() {
    // CODEX_EXAMPLE_MARKER: 这里的代码仅为示例，在你的工作之后应该修改为自己的逻辑，并删除该标识文本。
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long n = 0;
    cin >> n;
    cout << (n + 1) << '\n';
    return 0;
}
