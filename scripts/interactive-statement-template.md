# {{PROBLEM_TITLE}}

> CODEX_EXAMPLE_MARKER: 这里的内容仅为示例，在你的工作之后应该修改为自己的逻辑，并删除该标识文本。

## 题目内容

**这是一道交互题。**

这里是 `{{PROBLEM_NAME}}` 的初始化题面模板。请在正式出题时，用完整、严谨的题意替换这里的占位内容。

当前脚手架的交互逻辑仅用于保证目录结构、文件命名和基础编译流程是通的：

- 输入中给出一个隐藏整数。
- 你的程序需要输出一个整数作为最终答案。
- 当前占位交互器会检查这个答案是否与隐藏整数完全相同。

## 交互协议

交互器会先向你的程序提供一个整数。

你的程序读入这个整数后，输出一行一个整数作为答案，并及时刷新缓冲区。

## 样例

<!-- 样例一定要在这个块中，不要随便改格式！！！-->

```input1
42
```

```output1
42
```

## 样例解释

样例中的隐藏整数为 `42`，因此直接输出 `42` 即可。
