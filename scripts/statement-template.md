# {{PROBLEM_TITLE}}

> CODEX_EXAMPLE_MARKER: 这里的内容仅为示例，在你的工作之后应该修改为自己的逻辑，并删除该标识文本。

## 题目内容

这里是 `{{PROBLEM_NAME}}` 的初始化题面模板。请在正式出题时，将这里替换为完整、严谨、可直接发布的题面。

当前脚手架使用的是一个最小可验证占位题：

- 输入一个整数 `n`
- 输出这个整数本身

## 输入输出协议

### 输入

输入一行一个整数 `n`。

### 输出

输出一行一个整数，表示 `n` 本身。

## 样例

<!-- 样例一定要在这个块中，不要随便改格式！！！-->

```input1
5
```

```output1
5
```

## 样例解释

样例中输入为 `5`，因此输出 `5`。

