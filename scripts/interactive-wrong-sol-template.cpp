#include <bits/stdc++.h>

using namespace std;

int main() {
    // CODEX_EXAMPLE_MARKER: 这里的代码仅为示例，在你的工作之后应该修改为自己的逻辑，并删除该标识文本。
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long hidden = 0;
    cin >> hidden;
    cout << (hidden + 1) << endl;
    return 0;
}
